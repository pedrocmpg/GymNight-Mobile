# ============================================================================
# DEPENDÊNCIAS DO GYMNIGHT MOBILE BACKEND
# ============================================================================
# Todas as versões são fixas (pinned) para evitar quebras de compatibilidade.
#
# NOTA: bcrypt, passlib[bcrypt], python-jose[cryptography] e python-dotenv
# foram removidos. A autenticação agora é delegada ao Supabase Auth.
# A validação do JWT é feita pelo PyJWT (decode-only, HS256).
# A configuração de ambiente é gerenciada pelo pydantic-settings.
# ============================================================================

# --- Framework Web ---
fastapi==0.136.3
uvicorn==0.48.0
starlette==1.2.1

# --- Validação e Configuração ---
pydantic==2.13.4
pydantic_core==2.46.4
pydantic-settings==2.14.2  # Substitui python-dotenv; BaseSettings com validação automática
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# --- Autenticação JWT (decode-only, Supabase Auth) ---
PyJWT==2.8.0               # Validação stateless do JWT emitido pelo Supabase (HS256)

# --- Banco de Dados ---
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12
greenlet==3.5.1

# --- Utilitários ---
click==8.4.1
h11==0.16.0
anyio==4.13.0
idna==3.17
email-validator==2.3.0

# --- Criptografia (dependência transitiva do sistema, não usada diretamente) ---
cryptography==48.0.0
cffi==2.0.0
pycparser==3.0

# --- Testes ---
pytest==9.0.3
hypothesis==6.155.2
sortedcontainers==2.4.0
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0

# --- Documentação ---
annotated-doc==0.0.4
